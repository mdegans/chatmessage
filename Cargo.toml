[package]
name = "chatmessage"
version = "0.0.23"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
egui = { version = "0.27", optional = true }
thiserror = "1"

[features]
egui = ["dep:egui"]
client = []
server = []
dioxus = []
